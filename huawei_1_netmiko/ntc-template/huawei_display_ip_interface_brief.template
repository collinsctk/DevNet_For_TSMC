Value INTERFACE ([A-Z][a-zA-Z]*\d+(?:/\d+)*(?:\.\d+)?)
Value IPADDR (unassigned|\b(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\/\d+\b)
Value Physical_Status (up|down|\*down)
Value Protocol_STATUS (up|down)

Start
  ^${INTERFACE}\s+${IPADDR}\s+${Physical_Status}\s+${Protocol_STATUS} -> Record